name: Deploy to EC2

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

concurrency:
  group: deploy-backend
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/eduardsie/eventhub

permissions:
  contents: read
  packages: write

jobs:
  # ── JOB 1: Build & Push Image ─────────────────────────
  build:
    name: Build & Push Image
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── JOB 2: Deploy to EC2 ──────────────────────────────────────
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    needs: build
    steps:

      - uses: actions/checkout@v4

      - name: Add SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Copy files to server
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_HOST }} \
            "mkdir -p /home/ubuntu/app"
          scp -o StrictHostKeyChecking=no compose.yaml \
            ubuntu@${{ secrets.EC2_HOST }}:/home/ubuntu/app/compose.yaml
          if [ -f seed.sql ]; then
            scp -o StrictHostKeyChecking=no seed.sql \
              ubuntu@${{ secrets.EC2_HOST }}:/home/ubuntu/app/seed.sql
          fi
      - name: Deploy via SSH
        env:
          GHCR_USER: ${{ github.actor }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_HOST }} << 'ENDSSH'
            set -e

            APP_DIR="/home/ubuntu/app"
 
            cat > "$APP_DIR/.env" << 'ENVEOF'
          ${{ secrets.APP_ENV }}
          ENVEOF

            cat > "$APP_DIR/compose.yaml" << 'COMPOSEEOF'
          ${{ vars.COMPOSE_YAML }}
          COMPOSEEOF

            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io \
              -u ${{ github.actor }} --password-stdin

            cd "$APP_DIR"

            docker compose pull app

            docker compose up -d

            docker compose exec -T app alembic upgrade head

            if [ -f "$APP_DIR/seed.sql" ]; then
              EVENTS=$(docker compose exec -T db \
                psql -U postgres -d uni_db -tAc \
                "SELECT COUNT(*) FROM events;" || echo "0")
 
              EVENTS=$(echo "$EVENTS" | tr -d '[:space:]')
 
              if [ "$EVENTS" = "0" ]; then
                echo "DB is empty — running seed..."
                cat "seed.sql" | docker compose exec -T db psql -U postgres -d uni_db
                echo "Seed complete"
              else
                echo "DB already has $EVENTS events — skipping seed"
              fi
            fi

            docker image prune -f

          ENDSSH